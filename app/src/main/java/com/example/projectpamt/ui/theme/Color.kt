package com.example.projectpamt.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Brand Colors (Green Theme)
val GreenPrimary = Color(0xFF00754A)       // Primary brand color, header background, primary button, prices
val GreenSecondary = Color(0xFF53A285)     // Secondary accent color (user icon background)
val GreenMintBg = Color(0xFFEDF6F0)        // Soft mint background for text fields, quantity buttons, item cards
val GreenMintActive = Color(0xFFDCFCE7)    // Active bottom navigation indicator background

// Neutral Colors
val BackgroundSlate = Color(0xFFF8FAFC)    // Screen background (slate 50)
val SurfaceSlate = Color(0xFFF1F5F9)       // Secondary surface background (slate 100)
val BorderSlate = Color(0xFFE2E8F0)        // Divider / Border color (slate 200)

// Text Colors
val TextDark = Color(0xFF1E293B)           // Primary text (slate 800)
val TextMuted = Color(0xFF64748B)          // Secondary text (slate 500)
val TextPlaceholder = Color(0xFF94A3B8)    // Placeholder and inactive icons (slate 400)
val TextInverse = Color(0xFFFFFFFF)        // Text on dark green background

// Semantic Status Colors
val InfoBlueBg = Color(0xFFEFF6FF)         // Available stock badge background (blue 50)
val InfoBlueText = Color(0xFF2563EB)       // Available stock badge text (blue 600)
val WarningOrangeBg = Color(0xFFFFEDD5)    // Low stock badge background (orange 100)
val WarningOrangeText = Color(0xFFC2410C)  // Low stock badge text (orange 700)
val DangerRed = Color(0xFFEF4444)          // Delete / Cancel action (red 500)

// Quick Action Accent Colors
val ActionBlue = Color(0xFF3B82F6)         // Quick action 1 background (blue 500)
val ActionGreen = Color(0xFF22C55E)        // Quick action 2 background (green 500)
val ActionPurple = Color(0xFFA855F7)       // Quick action 3 background (purple 500)
val ActionOrange = Color(0xFFF97316)       // Quick action 4 background (orange 500)